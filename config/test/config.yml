imports:
    - { resource: ../default/config.yml }

core:
    require_dev_dependencies: true
    allow_install_dir: true

    extensions:
        enable_catalog: true

    session:
        force_sid: false
